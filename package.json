{
  "name": "whispy-server",
  "version": "1.0.0",
  "description": "Whispy — local signaling + static server. Zero infra cost.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "peer": "*"
  }
}
