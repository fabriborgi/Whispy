{
  "name": "whispy-server",
  "version": "1.0.0",
  "description": "Whispy PeerJS signaling server — self-hosted, zero infra cost",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "peer": "^0.9.1"
  }
}
